<Page
    x:Class="MagneticDoorSensorApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MagneticDoorSensorApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" >
            <TextBlock FontSize="50" Text="DOOR STATE" HorizontalAlignment="Center" />
            <TextBlock FontSize="18" HorizontalAlignment="Center" Foreground="Silver">
                <Run Text="Last updated: " />
                <Run Text="{x:Bind ViewModel.LastUpdated, Mode=OneWay}" />
            </TextBlock>
            <TextBlock FontSize="120" Text="{x:Bind ViewModel.State, Mode=OneWay}" HorizontalAlignment="Center"  />
        </StackPanel>
    </Grid>
</Page>
